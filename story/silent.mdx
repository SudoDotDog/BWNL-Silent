---
name: Silent
---

import { Silent, SilentConfig, SilentCommand } from "../src/index";
import { Playground } from 'docz'

# Silent

<Playground>
  <Silent 
    config={
        SilentConfig.create()
        .addCommand(SilentCommand.with('Reload Window', () => console.log('Reload Window')))
        .addCommand(SilentCommand.with('Something Else', () => console.log('Something Else')))
        .addCommand(SilentCommand.with('Very Short', () => console.log('Very Short')))
        .addCommand(SilentCommand.with('Anthoer Command That is really long', () => console.log('Anthoer Command That is really long')))
    }
  />
</Playground>
